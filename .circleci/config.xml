version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          start: docker run -p 3000:3000  metabase/metabase
          wait-on: 'http://localhost:3000'
